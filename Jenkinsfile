pipeline{
    agent any
    stages{
        stage('Build UI'){
            steps{
                echo "Build UI"
            }
        }
        stage('Middleware'){
            steps{
                echo "Build Middleware"
            }
        }
        stage('Database'){
            steps{
                echo "Build Database"
            }
        }
    }
}
